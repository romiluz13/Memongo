{
	"name": "@memongo/memory-bridge",
	"version": "2.0.1",
	"description": "Memongo bridge: stable facade for the memory engine used by HTTP API and MCP",
	"type": "module",
	"engines": {
		"node": ">=20.19.0"
	},
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/romiluz13/memongo.git",
		"directory": "packages/memory-bridge"
	},
	"author": "Rom Iluz",
	"keywords": [
		"memongo",
		"mongodb",
		"memory",
		"bridge"
	],
	"main": "./dist/memongo-bridge.js",
	"types": "./dist/memongo-bridge.d.ts",
	"files": [
		"dist",
		"README.md"
	],
	"exports": {
		".": {
			"types": "./dist/memongo-bridge.d.ts",
			"default": "./dist/memongo-bridge.js"
		}
	},
	"scripts": {
		"clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
		"build": "bun run clean && node ../../node_modules/typescript/bin/tsc -p tsconfig.json",
		"check-types": "node ../../node_modules/typescript/bin/tsc --noEmit -p tsconfig.json",
		"test": "vitest run",
		"prepublishOnly": "bun run build"
	},
	"dependencies": {
		"@memongo/lib": "^2.0.1",
		"@memongo/memory-engine": "^2.0.1"
	},
	"devDependencies": {
		"@memongo/tools": "workspace:*",
		"typescript": "^5.8.0",
		"vitest": "^4.1.10"
	},
	"homepage": "https://github.com/romiluz13/memongo#readme",
	"bugs": {
		"url": "https://github.com/romiluz13/memongo/issues"
	}
}
