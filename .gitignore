node_modules
.envrc
.direnv/
**/node_modules/
.env
.env.local
.env.*.local
.env.production
.env.staging
*.env
dist
coverage
*.tsbuildinfo
.pnpm-store
.DS_Store
**/.DS_Store
__pycache__/
**/__pycache__/
*.py[cod]
.turbo
**/.turbo/
.next
**/.next/
.open-next
**/.open-next/
.wrangler
**/.wrangler/
out
**/out/
artifacts/

# Local-only (not part of published OSS tree)
.claude/
.cc10x/
.cursor/
archive/

# Docker runtime
docker/mongodb/.runtime/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Benchmark datasets — fetched on demand and verified against the SHA-256
# pinned in benchmark-quality-contracts.ts. Too large to vendor (~265 MB).
benchmarks/data/

# Runtime benchmark checkpoints are resumable local state, not release artifacts.
benchmarks/results/checkpoints/

# Review scratch + runtime workflow logs (B15.2/B15.3)
.tmp-review/
*.events.jsonl

# Vitest JUnit output, emitted only when CI=true; parsed by
# scripts/ci/verify-tests-ran.mjs (skip-green guard).
test-results/
