# Legacy Notes

This page is the home for historical Memongo terminology, migration context, and old compatibility language that we no longer teach in the active product docs.

Use this page when you need to understand older references such as:

- OpenClaw-era workspace memory docs
- the old `openclaw`/gateway-centric configuration story
- historical comparison pages for Memongo versus upstream/default memory
- old onboarding and CLI notes that predate the current standalone product shape

Active documentation now lives in:

- `README.md`
- `apps/docs/`
- `docs/platform/`

If you are implementing or using Memongo today, start with `apps/docs/introduction.mdx` and `docs/platform/PLATFORM-README.md`.
